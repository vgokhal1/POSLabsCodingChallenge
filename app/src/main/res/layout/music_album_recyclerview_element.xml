<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/music_item_layout"
    android:layout_margin="8dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.v7.widget.CardView
        android:id="@+id/music_album_imageview_holder"
        app:cardCornerRadius="2dp"
        app:cardElevation="8dp"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/photo_album_imageview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/transparent"/>

    </android.support.v7.widget.CardView>

    <TextView
        android:id="@+id/music_album_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:layout_margin="8dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/music_album_imageview_holder"
        android:text="Portrait Title"
        android:textSize="14sp" />

</RelativeLayout>